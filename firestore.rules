rules_version = '2';
// Narrative and hardening notes: docs/security.md
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // MVP: open access (single user, no auth)
      // Lock this down when adding Firebase Auth
      allow read, write: if true;
    }
  }
}
